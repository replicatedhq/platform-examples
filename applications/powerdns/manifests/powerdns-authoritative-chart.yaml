apiVersion: kots.io/v1beta2
kind: HelmChart
metadata:
  name: powerdns-authoritative
spec:
  chart:
    name: powerdns-authoritative
    chartVersion: 0.0.3

  values:
    ingress:
      admin:
        hosts:
          - host: repl{{ ConfigOption "pdnsadmin_ingress_host" }}
            paths:
              - path: /
                backend:
                  serviceName: pdns-admin
                  servicePort: 80
