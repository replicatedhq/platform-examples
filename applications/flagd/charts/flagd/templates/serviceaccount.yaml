{{- if .Values.flagd.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "flagd.serviceAccountName" . }}
  labels:
    {{- include "flagd.labels" . | nindent 4 }}
  {{- with .Values.flagd.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
