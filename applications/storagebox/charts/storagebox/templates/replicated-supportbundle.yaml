apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "storagebox.labels" . | nindent 4 }}
    troubleshoot.sh/kind: support-bundle
  name: "{{ .Release.Name }}-supportbundle-config"
stringData:
  support-bundle-spec: |
    {{- include "storagebox.supportbundle" . | nindent 4 }}
